
            const secend = document.querySelector(".data");

                const result = `

                `;
                data.insertAdjacentHTML("beforeend", result);
            });
        })
        .catch(error => console.error('Error fetching the data:', error));
});
